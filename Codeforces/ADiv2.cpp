#include <bits/stdc++.h>
using namespace std; 
#define int long long 
#define endl cout<<"\n" 
template<class X, class Y> coin(X a, Y b) 
{
    return (2*a - b); 
}
void solve() 
{
    int a,b; 
    cin >> a >> b; 
    if (a >= b) {
        cout<<a; 
        endl; 
        return; 
    }
    int ans = coin(a,b); 
    if (ans >)
}