#include <stdio.h> 
#include <iostream>
#include <string> 
#include <bits/stdc++.h>
using namespace std;
int main() 
{
    int n,m,k; cin>>n>>m>>k; 
    string s; cin>>s; 
    s =  "L" + s + "L"; 
    n+=2; //Increase the Length of String by 2 
    vector<int> a(n); 
    for (int i=0; i<n; i++) 
    {
        if (s[i] == 'C') a[i] = '0'; 
        else if (s[i] == 'L') 
        {
            for (int j = )
        }
    }

    return 0; 
}