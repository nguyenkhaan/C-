#include <bits/stdc++.h>
using namespace std; 
#pragma GCC optimize("O3")
const int maxN = 2e5 + 3 
void solve() 
{
    int n,k; 
    cin >> n >> k; 
    vector<int> 6
    for (int i = 0; i < k; ++i) cin 
}